here i performed random forest regression including linear regression in a dataset called boston in which medv is taken as target variable then there are random datsets for performing different models including classifier models